- 前端通过 `@tauri-apps/api` 的 `invoke` 调用 Rust 命令，所有跨进程数据以 serde 序列化的 JSON 结构体传递
- 前端使用 Composition API 的 composable（如 `useMarketData`）封装轮询逻辑，组件仅消费响应式状态
- Vite 配置固定端口 1420 并通过 `TAURI_DEV_HOST` 注入 HMR 地址，确保与 Tauri 窗口调试一致