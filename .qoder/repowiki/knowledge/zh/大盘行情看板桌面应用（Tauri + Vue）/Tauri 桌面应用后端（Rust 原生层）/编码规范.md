- 通过 `#[tauri::command]` 宏将 Rust 函数暴露给前端调用，异步命令返回 `Result<T, String>` 以便错误信息透传到前端。
- 外部 HTTP 请求统一在独立模块（如 `market.rs`）中实现，并通过 `pub async fn` 暴露给 `lib.rs` 的命令层调用，保持命令层薄且只负责参数绑定与错误转换。
- 对外部不可信文本解析（如腾讯财经 GBK 响应）采用逐行过滤 + 长度校验 + 安全数值转换（`parse_f64` 默认回退为 0.0）的防御式解析模式。